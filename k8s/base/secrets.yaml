apiVersion: v1
kind: Secret
metadata:
  name: auth-secrets
  labels:
    app.kubernetes.io/part-of: online-shopping
    app.kubernetes.io/component: authentication
    app.kubernetes.io/managed-by: kustomize
type: Opaque
data:
  # Base64-encoded values (NOT encryption — just encoding)
  # In production, use a secrets management solution (Vault, External Secrets Operator)
  # To encode: echo -n "your-value" | base64
  # To decode: echo "encoded-value" | base64 --decode
  jwt-secret: c3VwZXItc2VjcmV0LWp3dC1rZXktZm9yLXNob3BwaW5nLWFwcC0yMDI2
